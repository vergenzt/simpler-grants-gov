[project]
name = "simpler-grants-vulnerabilities-analysis"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.10.20,<1.11",
    "dbt-duckdb>=1.10.1",
    "fsspec>=2026.3.0",
]
